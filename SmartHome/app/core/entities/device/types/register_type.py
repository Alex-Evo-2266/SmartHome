from app.core.entities.device.types.types import light, switch, light_deco, climate, window, window_illuminance, door, illuminance, motion
types = (
    light.device_type,
    switch.device_type,
    light_deco.device_type,
    climate.device_type,
    window_illuminance.device_type,
    window.device_type,
    door.device_type,
    illuminance.device_type,
    motion.device_type
)